# 🔄 RESUME (Compact)

أنت في وضع RESUME. MASTER محمّل بالأعلى. لا تعد العمل المُنجَز.

## STATE_SNAPSHOT الأخير:
```json
<ألصق هنا>
```

## آخر 60 سطر من الكود (للسياق):
```html
<ألصق هنا — اختياري>
```

## التعليمة:
1. ابدأ بـ: `▶️ RESUMING — Worker:<id> from Phase <x+1>`
2. سطر `📌 Verified state:` ملخص ما تأكدت منه
3. سطر `🎯 Now proceeding with:` الـ phase التالي
4. أنتج phase واحد فقط (≤ 600 سطر)
5. اختم بـ CHECKPOINT + STATE_SNAPSHOT جديد

## لو ضاع SNAPSHOT:
أعطني آخر phase تتذكر إنه اكتمل + اسم Worker. سأعيد بناء snapshot بالفحص.

ابدأ.
