# `motion/` — Motion modules

ÊLAN v4 — Pillar α / Stage 3 placeholder.

Planned modules (per `../MANIFEST.md`):

- `choreo.js` — δ5 view transitions + cross-page choreography
- `countup.js` — number ticker (replaces `upg-countup-1.js`)
- `reduced.js` — δ6 `prefers-reduced-motion` gate (single source of truth)

Migration happens in Pillar δ (KINETIC SHELL).
